const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="links">
                <a href="/">Homepage</a>
                <a href="/signup">Sign-up</a>
                <a href="/login">Login</a>

            </div>
        </nav>
    );
}

export default Navbar;